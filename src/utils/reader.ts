

import { readFile, writeFile } from "node:fs";
import path from "node:path"

const DB_PATH = path.join(process.cwd(), "./db")
const DB_MOVIES = path.join(process.cwd(), "./db/movies.json", "utf-8")


function readMovies(){
    
}